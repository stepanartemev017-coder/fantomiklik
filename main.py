import telebot
from telebot import types
from groq import Groq
import sqlite3
import json

# --- КОНФИГУРАЦИЯ ---
TOKEN = '8749709641:AAHZLNTR7afwWBGKjQLuJAnHUYOdTKT9_fo'
AI_KEY = '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'

bot = telebot.TeleBot(TOKEN)
client = Groq(api_key=AI_KEY)

# --- БАЗА ДАННЫХ ---
def db_op(query, params=()):
    conn = sqlite3.connect('memory.db')
    cursor = conn.cursor()
    cursor.execute(query, params)
    res = cursor.fetchone()
    conn.commit()
    conn.close()
    return res

conn = sqlite3.connect('memory.db')
conn.execute('CREATE TABLE IF NOT EXISTS history (chat_id INTEGER PRIMARY KEY, messages TEXT, state TEXT)')
conn.close()

# --- ЛИЧНОСТЬ ИИ ---
SYSTEM_PROMPT = (
    "Ты — Ethera, профессиональный ассистент чаттера на Fansly. "
    "Пользователь — твой босс и напарник. Общайся с ним мило, тепло и сдержанно. "
    "Твоя задача: помогать ему писать рассылки и ответы фанам. "
    "ПРАВИЛО ДЛЯ ФАНОВ: Пиши СТРОГО НА 'ТЫ', обращаясь к одному человеку лично. "
    "Используй структуру: Текущий контекст -> Игривый вопрос."
)

# --- 1. СПИСОК ВОПРОСОВ (Знакомство) ---
KNOWING_LIST = (
    "🤝 **СПИСОК ВОПРОСОВ ДЛЯ ЗНАКОМСТВА**\n\n"
    "1. `Как тебя зовут?` \n2. `Сколько тебе лет?` \n3. `Из какого ты города?` \n4. `Кем ты работаешь?` \n5. `Нравится работа?` \n"
    "6. `Как твой день прошел?` \n7. `Ты сейчас отдыхаешь?` \n8. `Что сейчас слушаешь?` \n9. `Твой рост?` \n10. `Веришь в судьбу?` \n"
    "11. `Активный отдых или диван?` \n12. `Твое любимое хобби?` \n13. `Что тебя смешит?` \n14. `Что ценишь в людях?` \n15. `Куда позовешь на свидание?` \n"
    "16. `О чем мечтаешь в тишине?` \n17. `Что во мне зацепило?` \n18. `Веришь в химию через экран?` \n19. `Любишь, когда тебя дразнят?` \n20. `Какая часть моего тела манит?` \n"
    "21. `Умеешь делать массаж?` \n22. `Ты нежный или властный?` \n23. `Твой любимый запах?` \n24. `Любишь обниматься?` \n25. `Твоя смелая фантазия?` \n"
    "26. `Доминировать или подчиняться?` \n27. `Что заводит мгновенно?` \n28. `Как относишься к ролевым?` \n29. `Что хочешь со мной сейчас?` \n30. `Твое главное желание на ночь?` \n\n"
    "*(Нажми на вопрос, чтобы скопировать)*"
)

# --- 2. 10 СЦЕНАРИЕВ СЕКСТИНГА ---
SEXTING_LIST = (
    "🔥 **10 СЦЕНАРИЕВ ПРОГРЕВА**\n\n"
    "1. **'СЛУЧАЙНЫЙ КАДР'**\n`Ой, пересматривала сейчас галерею и нашла одно фото... я тут такая настоящая. Показать?` \n*Дальше:* Описывай свои чувства, наряд или то, как ты скучаешь.\n\n"
    "2. **'ВЫБОР ОБРАЗА'**\n`Хочу сегодня вечером быть особенной для тебя... Поможешь выбрать белье под это платье?` \n*Дальше:* Описывай два цвета или фасона и спрашивай его мнение.\n\n"
    "3. **'ГОРЯЧИЙ СЕКРЕТ'**\n`Тссс... я сейчас в людном месте, но на мне нет белья. Это наше маленькое преступление.` \n*Дальше:* Рассказывай, как тебе волнительно и как ты чувствуешь взгляды прохожих.\n\n"
    "4. **'ПОСЛЕ ДУША'**\n`Я только что из душа, и тут так прохладно... Хочется, чтобы кто-то теплый был рядом.` \n*Дальше:* Описывай капли воды на коже и то, как бы ты к нему прижалась.\n\n"
    "5. **'СОН'**\n`Мне приснился очень яркий сон... и ты там был не совсем в одежде. 😊` \n*Дальше:* Рассказывай детали сна, постепенно повышая градус.\n\n"
    "6. **'ПРИКОСНОВЕНИЯ'**\n`Мои руки сегодня такие нежные... Хочу прикоснуться к тебе, с какого места мне начать?` \n*Дальше:* Переходи к детальному описанию тактильных ощущений.\n\n"
    "7. **'БЛИЗОСТЬ'**\n`В комнате так тихо, я слышу только свое дыхание и мысли о тебе...` \n*Дальше:* Спрашивай, чувствует ли он это напряжение, и переходи к фантазиям.\n\n"
    "8. **'ИГРА'**\n`Давай сыграем? Ты говоришь свое самое грязное желание, а я говорю, как я его исполню.` \n*Дальше:* Поощряй его откровенность и описывай свои действия в ответ.\n\n"
    "9. **'НАКАЗАНИЕ'**\n`Я сегодня была очень вредной девочкой... Кажется, мне нужно серьезное наказание. Справишься?` \n*Дальше:* Переходи к теме легкого доминирования и власти.\n\n"
    "10. **'ШЕПОТ'**\n`Приблизься к экрану... Я хочу прошептать тебе кое-что, от чего у тебя пойдут мурашки.` \n*Дальше:* Пиши сообщения так, будто ты шепчешь ему на ушко самые интимные вещи."
)

# --- 3. 10 ПРОМТОВ ДЛЯ ИИ ---
PROMPTS_LIST = (
    "📝 **10 ПРОМТОВ ДЛЯ РАССЫЛОК**\n\n"
    "1. `Сделай 5 рассылок на тему 'Уютный вечер'. Пиши на ТЫ. Структура: контекст + вопрос.`\n"
    "2. `Придумай 5 рассылок 'Я в магазине белья'. Пиши игриво на ТЫ, создай интригу.`\n"
    "3. `Накидай 5 утренних рассылок 'Только проснулась'. Тон нежный, обращение на ТЫ.`\n"
    "4. `Сделай рассылку-байтер: я начала что-то рассказывать и 'отвлеклась'. Создай любопытство.`\n"
    "5. `Придумай рассылку про выбор фильма на вечер, чтобы фан начал советовать.`\n"
    "6. `Сделай 3 варианта рассылки про то, как я готовлю ужин в одной футболке.`\n"
    "7. `Напиши рассылку 'Скучаю': я нашла вещь, которая напомнила мне о нем.`\n"
    "8. `Сделай рассылку про плохую погоду и желание согреться вдвоем.`\n"
    "9. `Напиши 3 дерзких рассылки для тех, кто давно не отвечал.`\n"
    "10. `Придумай рассылку 'Секрет': я хочу поделиться чем-то личным только с ним.`"
)

# --- КЛАВИАТУРА ---
def main_keyboard():
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True, row_width=2)
    markup.add(
        types.KeyboardButton("🤖 ИИ Ассистент"),
        types.KeyboardButton("🤝 Знакомство"),
        types.KeyboardButton("🔥 Секстинг"),
        types.KeyboardButton("📝 Промты ИИ")
    )
    return markup

# --- ОБРАБОТЧИКИ ---
@bot.message_handler(commands=['start', 'menu'])
def cmd_start(message):
    db_op('INSERT OR REPLACE INTO history VALUES (?, ?, ?)', (message.chat.id, '[]', 'main'))
    bot.send_message(message.chat.id, "Ethera на связи. Инструменты в клавиатуре ниже:", reply_markup=main_keyboard())

@bot.message_handler(func=lambda message: True)
def handle_msg(message):
    chat_id = message.chat.id
    text = message.text

    if text == "🤖 ИИ Ассистент":
        db_op('UPDATE history SET state="ai" WHERE chat_id=?', (chat_id,))
        bot.send_message(chat_id, "🦾 Режим ИИ активен. Отправь ситуацию или промт:")
        return
    elif text == "🤝 Знакомство":
        bot.send_message(chat_id, KNOWING_LIST, parse_mode="Markdown")
        return
    elif text == "🔥 Секстинг":
        bot.send_message(chat_id, SEXTING_LIST, parse_mode="Markdown")
        return
    elif text == "📝 Промты ИИ":
        bot.send_message(chat_id, PROMPTS_LIST, parse_mode="Markdown")
        return

    # Логика работы с ИИ
    res = db_op('SELECT messages, state FROM history WHERE chat_id=?', (chat_id,))
    if res and res[1] == "ai":
        bot.send_chat_action(chat_id, 'typing')
        history = json.loads(res[0])
        history.append({"role": "user", "content": text})
        
        try:
            completion = client.chat.completions.create(
                model="llama-3.3-70b-versatile",
                messages=[{"role": "system", "content": SYSTEM_PROMPT}] + history,
                temperature=0.8
            )
            answer = completion.choices[0].message.content
            history.append({"role": "assistant", "content": answer})
            db_op('UPDATE history SET messages=? WHERE chat_id=?', (json.dumps(history[-15:]), chat_id))
            bot.reply_to(message, answer)
        except Exception as e:
            bot.reply_to(message, "Ошибка связи с ИИ. Попробуй позже.")
    else:
        bot.send_message(chat_id, "Выбери раздел в меню ниже:", reply_markup=main_keyboard())

if __name__ == '__main__':
    bot.infinity_polling()
